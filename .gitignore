# סודות - לעולם לא להעלות ל-Git
.env

# נתוני ריצה מקומיים (היסטוריית חיפושים, בקשות שמורות)
data/

# סביבות פייתון וירטואליות
venv/
.venv/
env/

# קבצי פייתון זמניים
__pycache__/
*.pyc

# קבצי מערכת הפעלה / IDE נפוצים
.DS_Store
Thumbs.db
.vscode/
.idea/

# קובץ שיירי ריק שנוצר בטעות בניסיון commit קודם (לא ניתן למחיקה בסביבה זו)
git.bak

# מודול ישן שהלוגיקה שלו אוחדה בתוך app.py (parse_free_text_requests) כדי
# למנוע תלות יבוא נפרדת שעלולה לקרוס בשרת אם הקובץ לא יעלה כראוי
special_requests.py
